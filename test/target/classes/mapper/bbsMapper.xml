<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bbs">
	
	
	<insert id="insert">
		insert into board values (#{id}, #{content}, #{title},#{user})
	</insert>
	<update id="update" parameterType="bdto">
		update board set title = #{title} where user = #{user}
	</update>	
	<delete id="delete" parameterType="bdto">
		delete from board where id = #{id} and user = #{user}
	</delete>
	<select id ="search" resultType="bdto">
		select id,content,title,user
		from board
		where id = #{id}
	</select>
</mapper>